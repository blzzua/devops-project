{% extends "_main.jinja" %}

{% block title %} Login {% endblock %}

{% block main %}

<form class="text-center" action="{{ url_for("login_user") }}" method="post">
    {{ form.csrf_token }}
    <p>
        {{ form.username.label }}
        {{ form.username() }}
    </p>
    <p>
        {{ form.password.label }}
        {{ form.password() }}
    </p>
    <input class="btn btn-primary" type="submit" value="login">
</form>

{% endblock %}
