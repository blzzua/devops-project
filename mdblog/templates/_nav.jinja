
  <nav class= "text-center">
    <ul class="text-primary list-inline mx-auto justify-content-center"  style="list-style: none;">
      <li> <a class= "text-decoration-none" href="{{ url_for("view_about") }}">About</a> </li>
      <li> <a class= "text-decoration-none" href="{{ url_for("view_articles") }}">Articles</a> </li>
      <li> <a class= "text-decoration-none" href="{{ url_for("view_admin") }}">Admin</a> </li>
      {% if "logged" in session %}
        <li>
            <form action="{{ url_for("logout_user") }}" method="post">
                <input class="btn btn-danger" type="submit" value="logout">
            </form>
        </li>
      {% endif %}
    </ul>
  </nav>
